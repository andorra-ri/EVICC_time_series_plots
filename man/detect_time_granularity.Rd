% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annex_functions.R
\name{detect_time_granularity}
\alias{detect_time_granularity}
\title{Detect Time Granularity}
\usage{
detect_time_granularity(time_vector)
}
\arguments{
\item{time_vector}{A vector of \code{Date}, \code{POSIXt}, or coercible character timestamps.
If not already in date format, it will be coerced via \code{as.Date()}.}
}
\value{
A character string indicating the granularity: one of \code{"daily"}, \code{"weekly"},
\code{"monthly"}, \code{"yearly"}; or \code{NULL} if detection fails or input is invalid.
}
\description{
Attempts to classify the temporal granularity of a time vector by analyzing
the interval between consecutive dates. Automatically converts character input
to \code{Date} class where needed.
}
\details{
This function returns a classification as one of: \code{"daily"}, \code{"weekly"},
\code{"monthly"}, or \code{"yearly"}, based on typical interval thresholds.
}
\examples{
detect_time_granularity(as.Date(c("2020-01-01", "2020-01-02", "2020-01-03")))
# Returns: "daily"

detect_time_granularity(as.Date(c("2020-01-01", "2020-02-01", "2020-03-01")))
# Returns: "monthly"

detect_time_granularity(as.Date(c("2010-01-01", "2011-01-01", "2012-01-01")))
# Returns: "yearly"

detect_time_granularity(c("2020-01-01", "2020-01-08", "2020-01-15"))
# Returns: "weekly"

detect_time_granularity("not a date")
# Returns: NULL (with message)

}
