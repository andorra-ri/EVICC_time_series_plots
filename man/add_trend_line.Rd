% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_trend_line_plot.R
\name{add_trend_line}
\alias{add_trend_line}
\title{Add a Linear Trend Line to a Plotly Time Series Plot}
\usage{
add_trend_line(
  p,
  trend,
  df,
  time_var,
  value_var,
  color = dict_color[["trend_line_color"]]
)
}
\arguments{
\item{p}{A plotly plot object to which the trend line will be added.}

\item{trend}{Logical; if TRUE, the trend line is added.}

\item{df}{A data frame containing the time and value variables.}

\item{time_var}{Character; name of the time variable column (must be coercible to numeric).}

\item{value_var}{Character; name of the numeric value variable (or variable name if long format).}

\item{color}{Character; color for the trend line. Defaults to \code{dict_color[["trend_line_color"]]}.}
}
\value{
A plotly object with the trend line added (if \code{trend = TRUE}).
}
\description{
Fits a simple linear regression model to a time series and overlays
the predicted trend line on an existing plotly plot.
}
\details{
Automatically detects long-format data (e.g., from \code{pivot_longer}) and filters accordingly.
}
\examples{
df <- data.frame(time = as.Date("2020-01-01") + 0:9, value = 1:10 + rnorm(10))
p <- plotly::plot_ly(df, x = ~time, y = ~value, type = "scatter", mode = "lines")
add_trend_line(p, TRUE, df, "time", "value")
}
