% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_base_plot.R
\name{get_base_plot}
\alias{get_base_plot}
\title{Create a Base Plotly Time Series Plot (Line or Bar)}
\usage{
get_base_plot(
  df,
  time_var,
  value_var,
  plot_type = c("line", "bar"),
  color_per_value_var = FALSE,
  dict_color = dict_color,
  ylabel = ylabel
)
}
\arguments{
\item{df}{A data frame containing the input time series data.}

\item{time_var}{Character; name of the time variable column (must exist in \code{df}).}

\item{value_var}{Character; name of the numeric variable to be plotted.}

\item{plot_type}{Character; either \code{"line"} or \code{"bar"} to define plot style.}

\item{color_per_value_var}{Logical; if \code{TRUE}, use positive/negative colors. Defaults to \code{FALSE}.}

\item{dict_color}{Named list of color values. Must include:
\describe{
\item{main_color}{Used if \code{color_per_value_var = FALSE}}
\item{positive_value_color}{Color for positive values (if conditional coloring is used)}
\item{negative_value_color}{Color for negative values (if conditional coloring is used)}
}}
}
\value{
A \code{plotly} object representing the time series plot.
}
\description{
This function generates a Plotly time series chart based on a given data frame,
using either line or bar chart styles. It optionally supports conditional coloring
based on the sign of the values.
}
\examples{
dict_colors <- list(
  main_color = "steelblue",
  positive_value_color = "green",
  negative_value_color = "red"
)
df <- data.frame(
  time = Sys.Date() + 1:10,
  value = rnorm(10)
)
get_base_plot(df, "time", "value", plot_type = "bar", color_per_value_var = TRUE, dict_color = dict_colors)
}
